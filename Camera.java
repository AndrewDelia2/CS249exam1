public class Camera extends PhotographyStore
{
        public Camera(String initialType, int initialNumber)
        {
                super(initialType, initialNumber);
        }

        public Camera(String initialType)
        {
                super(initialType);
        }

        public Camera(int initialNumber)
        {
                super(initialNumber);
        }

        public Camera()
        {
                super();

        }
}
