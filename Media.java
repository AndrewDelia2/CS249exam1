public class Media extends PhotographyStore
{
        public Media(String initialType, int initialNumber)
        {
                super(initialType, initialNumber);
        }

        public Media(String initialType)
        {
                super(initialType);
        }

        public Media(int initialNumber)
        {
                super(initialNumber);
        }

        public Media()
        {
                super();
        }
}
